# GSD-Light

A lightweight, internal spec-driven development system for Claude Code (Opus 4.7), built on native Claude Code primitives (skills, subagents, hooks) with no custom CLI, SDK, or installer.

Runs under Claude Code's normal permission model. Never run with --dangerously-skip-permissions.

See docs/ARCHITECTURE.md for the design.
